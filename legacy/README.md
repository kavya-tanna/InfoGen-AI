# Earlier prototype

The original Streamlit files are preserved in `streamlit/` for reference. They
are not part of the current app and are not the verified run path.

Run the active FastAPI application using the repository README. The active
frontend is `InfoGen_AI.html`; the backend lives in `backend/app/`.
